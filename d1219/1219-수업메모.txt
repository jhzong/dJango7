1. 댓글 쓰고
2. 댓글 등록 버튼 누르면
3. 자바스크립트가 ajax로 서버한테 댓글 저장시킨다.
4. django가 db에 댓글 적는다.(저장)
5. django가 화면에 띄운다..?
6. 그래서 새로고침 안해도 보임

# 댓글
1. comment app 생성
- setting.py 추가
- urls.py 추가
- comment>urls.py 추가
2. comment db생성
- comment>clist 추가
3. board>def view -> comment 101번 하단댓글 가져오기
- comment db에서 하단댓글list 가져오기
4. comment write 프로그램

## 상단에 추가
# import requests
# import json
# import pprint

# 공공 API 불러오기/출력
def list2(request):
    # 공공데이터 접속
    public_key=''
    # page_no=requests.GET.get('page_no')
    page_no=1
    url=f"serviceKey={public_key}&numOfRows=10&pageNo={page_no}&MobileOS=ETC&MobileApp=AppTest&arrange=A&_type=json"
    # url의 모든 태그를 가져옴
    # 공공데이터 정보 가져오기
    rel=requests.get(url)
    # 파일변환:str->json
    json_data=json.loads(rel.text)
    p_list=json_data['response']['body']['items']['item']
    print('[json데이터]=>',p_list[0])    
    context = {'result':'성공','list':p_list}
    return render(request,'board/list2.html',context)